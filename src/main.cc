#include <iostream>
#include "Timestamp.h"

int main(int argc, const char** argv) {
    std::cout << "fuck off!" << std::endl;
    std::cout << Timestamp::now().toString() << std::endl;
    return 0;
}